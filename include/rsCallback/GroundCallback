#ifndef RSCALLBACK_GROUNDCALLBACK_
#define RSCALLBACK_GROUNDCALLBACK_

#include <osg/Node>
#include <osg/NodeVisitor>

#include <rsSim/Sim>

namespace rsCallback {

	// class
	class GroundCallback : public osg::NodeCallback {
		// public functions
		public:
			GroundCallback(rsSim::Ground*);
			virtual ~GroundCallback(void) {};

			virtual void operator()(osg::Node*, osg::NodeVisitor*);

		// private data
		private:
			rsSim::Ground *_ground;
	};

} // namespace rsCallback

#endif // RSCALLBACK_GROUNDCALLBACK_

